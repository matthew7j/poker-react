class Player {
  constructor(id, name, chips, seatIndex, tableId) {
    this.id = id;
    this.name = name;
    this.chips = chips;
    this.seatIndex = seatIndex;
    this.tableId = tableId
  }
};

export default Player;